apiVersion: v1
kind: Service
metadata:
  name: driver-service
  namespace: default
  labels:
    app: driver-service
spec:
  type: ClusterIP
  selector:
    app: driver-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
